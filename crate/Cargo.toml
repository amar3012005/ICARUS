# mneme — Rust workspace.
# P1: mneme-probe (proof-of-physics probe binary + lib).
# Later phases add mseg-format, mnsw-index, mpq-codebook, mneme-node crates here.
[workspace]
resolver = "2"
members = ["mneme-probe"]

[workspace.package]
edition = "2021"
license = "Apache-2.0"
repository = "https://github.com/amar3012005/HIVEMIND"
rust-version = "1.74"

[workspace.dependencies]
# Reuse spine (see reference/OPENSOURCE_RECON.md). Pinned exact per global standards.
memmap2 = "=0.9.5"
zerocopy = { version = "=0.8.26", features = ["derive"] }
lz4_flex = "=0.11.3"
rayon = "=1.10.0"

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
