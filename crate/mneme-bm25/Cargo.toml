[package]
name = "mneme-bm25"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure BM25 lexical scoring for mneme -- no Shard, no napi, no pyo3, so it stays unit-testable with plain `cargo test` and shared identically by every language binding instead of drifting into per-binding copies."

[dependencies]
