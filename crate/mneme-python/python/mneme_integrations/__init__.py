"""Optional framework integrations for mneme_python. Each submodule imports its own framework
lazily (langchain-core / llama-index-core) so importing this package does not require either
unless you actually use that integration."""
