[package]
name = "mnsw-index"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "mneme .mnsw HNSW index — thin usearch wrapper (label = slot id). No bespoke HNSW."

[lib]
name = "mnsw_index"
path = "src/lib.rs"

[dependencies]
usearch.workspace = true

[dev-dependencies]
tempfile = "=3.13.0"
