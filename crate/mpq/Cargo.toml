[package]
name = "mpq"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "mneme .mpq Product Quantization codebook (per-org, M=128 K=256) + ADC + drift detection"

[lib]
name = "mpq"
path = "src/lib.rs"

[dependencies]
zerocopy.workspace = true
rayon.workspace = true

[dev-dependencies]
tempfile = "=3.13.0"
